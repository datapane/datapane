name: dp_test_script

parameters: [
  {"name": "param1", "type": "string", "default": "hello"},
  {"name": "param2", "type": "integer", "required": true}
]

repo: https://github.com/datapane/test

include:
  - sample.ipynb

requirements:
  - requests
